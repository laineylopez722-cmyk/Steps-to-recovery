/**
 * Shared test utilities
 *
 * Re-exports common test helpers for clean imports:
 * ```ts
 * import { createTestWrapper } from '@/__tests__/utils';
 * ```
 *
 * @module __tests__/utils
 */

export { createTestWrapper } from './createTestWrapper';
